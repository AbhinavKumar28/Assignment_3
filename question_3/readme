This is space for question 3. All the output is in program.pdf. There are 4 nodes, hence one code file for each such as node0.c, etc. prog3.c is the same as  distance_vector.c with some extra print statements for debugging.
cmake_minimum_required(VERSION 3.6)
project(DVR)

set(CMAKE_C_STANDARD 99)
set(CMAKE_C_COMPILER /usr/local/bin/gcc-6)
set(SOURCE_FILES prog3.c node0.c node1.c node2.c node3.c)
add_executable(DVR ${SOURCE_FILES})
